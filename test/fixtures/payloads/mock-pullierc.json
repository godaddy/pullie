{
  "plugins": [
    {
      "plugin": "reviewers",
      "config": {
        "commentFormat": "Requested review from %s"
      }
    },
    {
      "plugin": "requiredFile",
      "config": {
        "files": [
          {
            "path": "CHANGELOG.md",
            "message": "Missing CHANGELOG entry"
          }
        ]
      }
    },
    {
      "plugin": "welcome",
      "config": {
        "welcomeMessage": "o hai!"
      }
    }
  ]
}
