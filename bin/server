#!/usr/bin/env node
const argv = require('yargs').argv;

require('../lib').start({ configPath: argv.configPath }, function (err, app) {
  if (err) {
    throw err;
  }
  const address = app.servers.http.address();
  app.log.info(`App has started on port ${address.port}`);
});

